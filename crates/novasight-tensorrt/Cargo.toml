[package]
name = "novasight-tensorrt"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
ffi = []
gpu-frame = ["ffi"]

[dependencies]
novasight-core = { path = "../novasight-core" }
thiserror.workspace = true

[build-dependencies]
cc = "1.3"
