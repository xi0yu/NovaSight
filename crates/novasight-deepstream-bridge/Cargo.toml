[package]
name = "novasight-deepstream-bridge"
version = "0.1.0"
edition = "2024"

[features]
default = []
ffi = []

[dependencies]
novasight-core = { path = "../novasight-core" }
thiserror.workspace = true

[build-dependencies]
cc = "1.3"
pkg-config = "0.3"
