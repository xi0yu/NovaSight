[package]
name = "novasight-tensorrt"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
ffi = []

[dependencies]
novasight-core = { path = "../novasight-core" }
novasight-jetson-preprocess = { path = "../novasight-jetson-preprocess" }
thiserror.workspace = true

[build-dependencies]
cc = "1.3"
