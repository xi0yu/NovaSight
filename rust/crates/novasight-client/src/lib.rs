//! `novasight-client` — the typed client library used by
//! `novasightctl` to talk to the daemon over the Unix Domain Socket.
//!
//! Commit 1 ships the empty crate skeleton. The Unix socket
//! transport and method surface land in Commit 6.

#![forbid(unsafe_code)]
