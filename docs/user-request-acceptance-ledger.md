# 用户诉求逐项验收（当前工作区，2026-09-22）

状态语义：`代码已实现`只代表当前源码和列出的主机检查；`待实机`不能由模拟 API、旧提交收据或页面文案代替；`未完成`不能写成“已支持”。工作区有未提交修改时以 `HEAD + diff` 为准。复核时先记录提交 SHA、服务二进制身份和配置/effective 版本。

总体结论：**尚未逐项完成**。源码与隔离测试包已覆盖主要操作，但非空目标实测、高负载页面通道、原运行目录迁移、公平性阻断和有监督的物理输出/断连仍缺证据；不得将已通过的主机测试合并为产品验收。

截至 `06b69f3` 的主机检查：Rust 目标跟踪 40、流水线单元 8、Store 目录/文件 2、API 隔离 HTTP 10 均通过；Web 生产构建与 117 项单元测试通过，桌面/窄屏模拟浏览器原有 58 项及新增移动成功/失败 4 项通过。`ff1046f` 新增网关请求关联日志和对应 1 项针对性通过的 Rust 测试；`d12a5002` 的两条 GitHub quality CI 均通过。后续模型使用状态筛选的本地检查为 23 项针对性单元测试、生产构建、桌面/窄屏浏览器各 1 项通过；`f9b2d2c7` 的两条 GitHub quality CI 也已通过。本次对“输出开启时启动/重连需再次同意”新增两条回归，`StudioConfig.test.tsx` 29 项通过；未重复运行完整套件。浏览器代理向未启动的开发后端发出的连接告警不代表真实后端验收。

Jetson 收据：工作树原有未跟踪 `config/`、`run/` 与 wheel 保留；`74f06c3` Rust/Web 构建、便携包组装和包内二进制哈希核对通过；包配置 `output_enabled=false`、`auto_connect=false`。隔离真实网关脚本通过临时授权登录、类别保存/删除与 effective 版本读回、文件夹创建与 Engine 移动。便携包真实浏览器已登录；页面检测到 1920×1080@240 并手选保存，类别瞄点 22→23 的弹窗/草稿/参数保存成功，文件夹创建和重名 HTTP 409 原始详情可见。首次 Kenny 256 Engine 切换实际完成后端部署却被旧前端 CUDA parser 常量误报失败；`2de1d5a` 前端修复同步后，真实页面成功切换 Kenny 320 再恢复 Kenny 256，显示 100% 和 CUDA 解析格式，重名错误的用户文案改为中文且异常中心保留原始错误。随后输出关闭的主链真实运行，采集页保存与运行态均显示 MJPEG 1920×1080@240，推理/结果约 202 FPS，ROI/模型输入 256×256；设备只读 V4L2 查询也为 MJPG 1920×1080、240.004 fps。最近检测仍是 0，故不把这些视作非空框/类别/NMS 的实证。以上页面收据基于测试包，不代表用户原运行目录已迁移或正在运行相同版本。`ff1046f` 源码已同步至 Jetson，网关二进制在测试包中重建并核对哈希；尝试开启一次隔离复现时 SSH 和页面端口同时失联，现阶段**不能断言测试服务已经停止或正在运行**，也不能断言失联由推理负载引起。失联前再次核实包配置中输出和自动连接均关闭。

| 用户诉求 | 当前判断与可核查入口 | 尚缺的证据或工作 |
| --- | --- | --- |
| 内部算法分类、责任、前后冲突、工作空间可视化地图 | [责任/风险地图](algorithm-self-check-map.md)链接到当前感知、目标、控制、交付代码，并列出 raw cls/身份、短暂保留/旧命令、两类预测、GPU 校验和输出同意的前后边界；旧交互地图标记为历史。准星超时、瞄点热更新各补一条回归检查。 | 公平性保护未实现；真实设备对照未完成；后端启动会从保存配置恢复输出门，系统级逐次同意尚无保证。 |
| YOLO→decode→NMS、为什么旧路走 CPU、全 GPU 且通用 | [正式 GPU 边界](gpu-image-pipeline.md)与[历史优化基线](pipeline-gpu-optimization-plan.md)区分诊断 CPU decoder 与正式 CUDA 路径。 | 当前支持的是列出的格式/模型契约，不是任意图片/任意 YOLO；当前提交没有真实非空目标全程收据。 |
| 有效检测结果、越界/排序/NMS/异常根因 | TensorRT [GPU 结果边界](../crates/novasight-tensorrt/src/gpu.rs)、[异常展示回归](../web/src/features/studio/StudioErrors.test.tsx)；Jetson 模型切换实测抓到前后端 CUDA parser 字段冲突，修复后已在新版真实页面复验。 | 非空检测的类别/坐标/排序及原始错误对照仍待验。 |
| 内部异常机制、前后端字段与用户可见的故障详情 | 异常中心保留原始错误与开发者详情；模型切换的 CUDA parser 字段冲突已由真实页面发现并修复，类别保存和目录重名也有真实 HTTP 回执。 | 尚未逐一穷尽所有 API 字段、按钮与失败路径；约 202 FPS 下状态通道降级/请求超时根因未确认，不能称为全链路异常闭环。 |
| 采集手选 MJPEG 1k 240，不能退回默认 MJPEG | [采集选择合同](../crates/novasight-core/src/capture.rs)；Jetson 测试包真实页面检测、选择、保存并运行读回 1920×1080@240，设备只读查询同档，推理有效输入约 202 FPS。 | 用户原运行目录的配置未替换；有效输入 FPS 不等于采集卡原始帧率。 |
| 单个临时授权码、临时码允许硬件控制、启动错误 | [启动说明](../README.md)和[授权合同](../crates/novasight-store/src/license.rs)；Jetson 便携包启动与单码 LAN 页面登录通过，启动提示无冒号；输出门配置核对为关闭。 | 临时授权下的物理输出/断连回执未测试，不把登录等同于硬件控制验收。 |
| kmNet 连接/断开、输出按钮及物理确认 | [输出门](../crates/novasight-pipeline/src/runtime.rs)与[前端问题 17](frontend-qa-2026-09-21.md)避免导入配置绕过确认。已允许输出时，Studio 对启动/重连先确认，CLI 需显式传 `--allow-physical-output`；[控制 API](../crates/novasight-api/src/control.rs)对缺少确认的启动、重启和 kmNet 连接返回 428，不阻挡停止/断连。录制设备 API 定向测试、原授权回归、网关请求头过滤测试、29 项前端测试、Web 构建、CLI 编译与选项帮助均通过。 | daemon 仍从保存配置恢复输出门，请求标记不是真人在场证明；模型发布/配置重载等内部重启路径和实机断连回执尚未验收。新版确认尚未在 Jetson 真实页面验证。 |
| 多目标、大小/远近/cls/遮挡、弱化 ByteTrack | [算法责任图](algorithm-self-check-map.md)说明 raw cls 是软关联项，选择按业务得分，丢目标不交付旧目标；40 项主机跟踪合同已通过。 | 快速遮挡、目标密集现场轨迹与手感待采样标定；没有证明数学最优。 |
| 模块划分、前后端字段、冗余与可维护性 | [架构与维护边界](../PROJECT_HEALTH_AUDIT.md)、[Web 契约](../web/src/contracts/model.ts)；本轮删除未被控制消费的可靠性字段。 | 未把整个仓库证明“无冗余”，需要按模块继续审查，避免大规模无目的重构。 |
| 旧文档清理、启动说明与专业建议 | [GPU 正式边界](gpu-image-pipeline.md)已把旧 CPU/DeepStream 路线标为历史，模型接入和启动说明有当前入口；算法/界面建议已转为责任图、交互问题台账和验收项。 | 尚未完成全仓历史文档与当前行为的逐条一致性清点；外部数学专家尚未参与，也未把建议误写成已证实的最优算法。 |
| 前端提示、同意、错误详情、15 个真实问题、三主题 | [20 项历史问题及对应回归](frontend-qa-2026-09-21.md)已复核；Jetson 真实页面确认目录创建、重名错误保留输入，中文原因显示在原处与异常中心，展开后有 HTTP 409/错误码/排查编号/原文；模型切换有显式确认与结果。窄屏异常入口补可读名称。本地模型库新增每次 24 个渐进呈现、使用状态数量和按已登记/待验证/仅查看筛选；23 项针对性单元测试、构建及桌面/窄屏浏览器筛选用例通过。物理输出开启时，参数保存、主链启动和 kmNet 重连现在均会先说明风险并等待同意；取消不发对应请求。 | 新版参数确认、启动/重连确认与模型库尚未在 Jetson 真实页面复验；视觉偏好与首次整理体验不能由构建或模拟浏览器宣称验收。 |
| 模型管理的使用关系、文件夹、命名排序 | [模型接入边界](model-ingress.md)；已有自然排序/搜索/文件夹浏览，新增安全创建空文件夹；未登记、无 manifest 的 Engine 支持确认后移动/改名，已登记文件拒绝；主机与模拟页面覆盖成功/拒绝。Jetson 真实页面创建目录并读回，隔离真实网关移动 Engine 并读回；新版真实页面切换两个 Engine 成功。 | 已登记模型的路径同时被数据库、验证清单和部署引用使用，不能把现有安全拒绝改成普通文件重命名；若要支持迁移，需要单独的事务与回滚设计。同一真实浏览器中的文件移动和大量模型首次整理体验仍待验。 |
| 类别配置/参数保存与热更新 | [配置 HTTP 合同](../scripts/ci/class-config-http-contract.py)通过；Jetson 真实页面调整瞄点并保存，反馈 `参数已保存并生效`、不再报 `aim`，物理输出未开启；随后在同一测试包中发布模型并运行主链。 | 瞄点修改对非空目标控制结果的影响未实测；用户原运行目录未替换。 |
| Git 同步、Jetson 部署和实战测试 | GitHub 已同步本地提交 `4ce5beee`；Jetson 源码仅同步至 `ff1046f`。Jetson 主包二进制构建于 `74f06c3`，Web 资产构建于 `06b69f3`，测试包网关二进制构建于 `ff1046f`。此前便携包真实页面已复验；失联前包配置仍为输出/自动连接关闭。 | 本次 SSH 端口检查仍未连通，Jetson 连接/服务状态未知；这不是用户原运行目录的进程，真实非空检测未验证。 |
| “光标吸着人走”的公平性异常 | [算法图](algorithm-self-check-map.md)把其作为外部判定风险，不将算法准确率等同合规。 | 尚无经用户/规则方确认的阈值与日志样本；仅考虑阻断/dry-run，不做规避判定的行为伪装。 |

遗漏/授权边界：没有请外部数学专家给出“最优”结论，也没有将游戏公平性外部规则偷换为目标选择目标；当前仅保留算法责任和风险图。UI 三主题在代码中有合同，但“2026 年受欢迎”或符合个人审美不是自动化测试可证明的事实。20 项前端问题是历史修复，不等于之后所有用户操作都已审完；模块/冗余只做了有调用证据的局部治理，不能声称全仓无冗余。原运行目录迁移和物理输出尚未获得可安全替代的验收，不能由隔离包推定。

验收顺序和未完成项在 [任务清单](../tasks/todo.md)。真实推理约 202 FPS 时，页面曾出现实时状态陈旧、WebSocket 降级和 `/api/runtime/state` 5 秒、模型目录 10 秒超时，稍后恢复；这不是正常完成标准，性能/实时通道根因仍待查。网关新增请求接收/响应头耗时与请求编号，待 Jetson 恢复连接后仅做一次有界对照复现。不以此表、主机测试或旧版文档替代真实运行证据。
