# 用户诉求逐项验收（当前工作区，2026-09-22）

状态语义：`代码已实现`只代表当前源码和列出的主机检查；`待实机`不能由模拟 API、旧提交收据或页面文案代替；`未完成`不能写成“已支持”。工作区有未提交修改时以 `HEAD + diff` 为准。复核时先记录提交 SHA、服务二进制身份和配置/effective 版本。

本轮主机检查：Rust 目标跟踪 40、流水线单元 8、Store 目录/文件 2、API 隔离 HTTP 10 均通过；Web 生产构建、原有 114 项单元测试及新增错误文案测试通过，桌面/窄屏模拟浏览器原有 58 项及新增移动成功/失败 4 项通过。浏览器代理向未启动的开发后端发出的连接告警不代表真实后端验收。

Jetson `74f06c3` 收据：工作树原有未跟踪 `config/`、`run/` 与 wheel 保留；当前 Rust/Web 构建、便携包组装和包内二进制哈希核对通过；包配置 `output_enabled=false`、`auto_connect=false`。隔离真实网关脚本通过临时授权登录、类别保存/删除与 effective 版本读回、文件夹创建与 Engine 移动。便携包真实浏览器已登录；页面检测到 1920×1080@240 并手选保存，页面读回 240；类别瞄点 22→23 的弹窗/草稿/参数保存成功；新建文件夹成功，重名 HTTP 409 的原始详情和排查编号可见。设备只读 V4L2 查询为 MJPG 1920×1080、240.004 fps。随后真实页面对 Kenny 256 Engine 的“验证并切换”完成后端部署、主链恢复，但前端因把 CUDA `parser_library=novasight_builtin_cuda` 误限定为旧 `novasight_builtin` 而报告失败；这是实测发现的契约缺陷，当前工作区已修正并补回归，待新版本同步复验。运行中的采集虽然转为运行中，但非空检测及真实协商帧率仍未证明。以上页面收据基于测试包，不代表用户原运行目录已迁移或正在运行相同版本。

| 用户诉求 | 当前判断与可核查入口 | 尚缺的证据或工作 |
| --- | --- | --- |
| 内部算法分类、责任、前后冲突、工作空间可视化地图 | [责任/风险地图](algorithm-self-check-map.md)链接到当前感知、目标、控制、交付代码；旧交互地图标记为历史。准星超时、瞄点热更新各补一条回归检查。 | 公平性保护未实现；真实设备对照未完成。 |
| YOLO→decode→NMS、为什么旧路走 CPU、全 GPU 且通用 | [正式 GPU 边界](gpu-image-pipeline.md)与[历史优化基线](pipeline-gpu-optimization-plan.md)区分诊断 CPU decoder 与正式 CUDA 路径。 | 当前支持的是列出的格式/模型契约，不是任意图片/任意 YOLO；当前提交没有真实非空目标全程收据。 |
| 有效检测结果、越界/排序/NMS/异常根因 | TensorRT [GPU 结果边界](../crates/novasight-tensorrt/src/gpu.rs)、[异常展示回归](../web/src/features/studio/StudioErrors.test.tsx)；Jetson 模型切换实测抓到前后端 CUDA parser 字段冲突并补合同测试。 | 新版页面复验、非空检测的类别/坐标/排序及原始错误对照仍待验。 |
| 采集手选 MJPEG 1k 240，不能退回默认 MJPEG | [采集选择合同](../crates/novasight-core/src/capture.rs)；Jetson 测试包真实页面检测、选择、保存并读回 1920×1080@240；设备只读查询也显示这一档。 | 包内未发布模型，主链未开始采集，不能称运行态协商已验证；用户原运行目录的配置未替换。 |
| 单个临时授权码、临时码允许硬件控制、启动错误 | [启动说明](../README.md)和[授权合同](../crates/novasight-store/src/license.rs)；Jetson 便携包启动与单码 LAN 页面登录通过，启动提示无冒号；输出门配置核对为关闭。 | 临时授权下的物理输出/断连回执未测试，不把登录等同于硬件控制验收。 |
| kmNet 连接/断开、输出按钮及物理确认 | [输出门](../crates/novasight-pipeline/src/runtime.rs)与[前端问题 17](frontend-qa-2026-09-21.md)避免导入配置绕过确认。 | 必须有人监督才可验证物理偏移、断连回执；此轮没有发送设备控制。 |
| 多目标、大小/远近/cls/遮挡、弱化 ByteTrack | [算法责任图](algorithm-self-check-map.md)说明 raw cls 是软关联项，选择按业务得分，丢目标不交付旧目标；40 项主机跟踪合同已通过。 | 快速遮挡、目标密集现场轨迹与手感待采样标定；没有证明数学最优。 |
| 模块划分、前后端字段、冗余与可维护性 | [架构与维护边界](../PROJECT_HEALTH_AUDIT.md)、[Web 契约](../web/src/contracts/model.ts)；本轮删除未被控制消费的可靠性字段。 | 未把整个仓库证明“无冗余”，需要按模块继续审查，避免大规模无目的重构。 |
| 前端提示、同意、错误详情、15 个真实问题、三主题 | [20 项历史问题及对应回归](frontend-qa-2026-09-21.md)已复核；Jetson 真实页面确认目录创建、重名错误保留输入、异常中心有 HTTP 409/错误码/排查编号/原文；新增中文冲突提示仍需部署复验。 | 当前页面 73 个模型一次铺满列表，尚不够易管；视觉偏好不能由构建或模拟浏览器宣称验收。 |
| 模型管理的使用关系、文件夹、命名排序 | [模型接入边界](model-ingress.md)；已有自然排序/搜索/文件夹浏览，新增安全创建空文件夹；未登记、无 manifest 的 Engine 支持确认后移动/改名，已登记文件拒绝；主机与模拟页面覆盖成功/拒绝。Jetson 真实页面创建目录并读回，隔离真实网关移动 Engine 并读回；真实模型切换找到 CUDA parser 前端契约旧值。 | 新版切换页面复验、已登记模型的路径迁移、同一真实浏览器中的移动仍未完成。 |
| 类别配置/参数保存与热更新 | [配置 HTTP 合同](../scripts/ci/class-config-http-contract.py)通过；Jetson 真实页面调整瞄点并保存，反馈 `参数已保存并生效`、不再报 `aim`，物理输出未开启。 | 包内无已发布模型，控制效果与运行链仍未实测；用户原运行目录未替换。 |
| Git 同步、Jetson 部署和实战测试 | GitHub/Jetson 已同步至 `74f06c3`；Jetson 当前源码、构建后的二进制、Web 资产、便携包及测试包进程分别核对。包内 launcher/daemon 哈希与构建产物一致。 | 新增中文冲突提示尚未提交/同步；这不是用户原运行目录的进程；真实非空检测未验证。 |
| “光标吸着人走”的公平性异常 | [算法图](algorithm-self-check-map.md)把其作为外部判定风险，不将算法准确率等同合规。 | 尚无经用户/规则方确认的阈值与日志样本；仅考虑阻断/dry-run，不做规避判定的行为伪装。 |

验收顺序和未完成项在 [任务清单](../tasks/todo.md)。实际浏览时，实时状态在模型准备等长操作期间短暂标为“运行数据已陈旧”，之后自行恢复；HTTP/进程持续正常，原因未定，需保留为观察项。不以此表、主机测试或旧版文档替代真实运行证据。
