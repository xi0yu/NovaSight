# NovaSight 用户手册

本文面向拿到 `NovaSight/` 交付包的使用者。它只说明怎么启动、授权、配置和排查；不要求用户自己编译项目，也不包含开发者命令。

## 1. 使用前准备

使用 NovaSight 前，请确认 Jetson 设备已经连接好：

- 视频输入设备：采集卡、摄像头或其他 `/dev/video*` 输入。
- 推理模型：推荐使用 `.engine` 模型文件。
- 控制设备：kmNet 设备和 Jetson 在同一可访问网络内。
- 显示与浏览器：Studio 可从同一局域网浏览器访问；有桌面环境时可自动打开本机浏览器。
- 正式授权凭证：正式包需要授权后才能进入完整工作台；测试包可能允许临时权限。

用户机只运行已经打好的包，不需要安装开发工具，也不需要执行任何编译命令。

## 2. 交付包内容

用户收到的目录一般长这样：

```text
NovaSight/
├── NovaSight
├── USER_MANUAL.md
├── README-USER.txt
├── bin/
├── web/
├── data/
├── logs/
└── run/
```

常用位置：

- `NovaSight`：唯一推荐的启动入口。
- `USER_MANUAL.md`：本用户手册。
- `data/novasight.yaml`：本机配置文件，通常通过界面修改。
- `data/models/`：放置本机模型文件。
- `logs/novasightd.log`：运行日志，排查问题时查看。
- `logs/novasight-web.log`：网页认证与 API 网关日志。
- `run/novasightd-ready.json`：daemon Unix IPC 就绪信息。
- `run/ready.json`：Web/API 启动成功后记录 Studio 地址。

删除整个 `NovaSight/` 目录，就会同时删除 NovaSight 自己创建的数据、日志和运行状态文件。

## 3. 第一次启动

进入交付包目录，然后运行：

```bash
cd NovaSight
./NovaSight
```

启动器会自动完成这些事：

- 创建缺失的 `data/`、`logs/`、`run/` 目录。
- 启动本机 NovaSight 后端服务。
- 等待服务就绪。
- 固定监听 `0.0.0.0:7351`，输出 `NovaSight Studio Web UI: http://0.0.0.0:7351/` 和对应的 LAN 地址，并在有桌面浏览器时尝试自动打开。
- 保持终端运行；按 `Ctrl+C` 会关闭包内后台服务。

如果浏览器没有自动打开，请使用终端输出的 LAN 地址，例如 `http://<Jetson-LAN-IP>:7351/`。不要把 Jetson 本机的 `127.0.0.1` 当成远程访问地址。`0.0.0.0` 代表服务监听所有网卡；远程浏览器实际访问时使用 Jetson 的局域网 IP。Jetson 没有桌面浏览器时，这是正常路径，不代表后端启动失败。

正常情况下，用户不需要直接运行 `bin/novasightd`，也不需要安装系统服务。

## 4. 授权

首次进入 Studio 时，如果还没有授权，会先进入授权页面。

正式使用：

1. 将授权凭证粘贴到“授权凭证”输入框。
2. 点击“激活正式授权”。
3. 状态显示“授权有效”后进入工作台。

测试使用：

- 如果页面显示“申请临时权限”，可以点击它进入测试授权。
- 临时权限只对当前运行进程有效，退出 NovaSight 后会失效。
- 正式包不开放临时权限。

退出授权：

1. 进入授权管理区域。
2. 点击“退出当前授权”。
3. 再次确认后，当前设备退出授权状态。

## 5. Studio 主界面

进入工作台后，左侧是主要页面：

- 采集：选择输入设备、采集格式、ROI 和预览帧率。
- 模型推理：查看当前模型、切换模型、调整置信度和 NMS。
- 控制：观察目标选择、控制误差和设备发送状态。
- 参数设置：管理类别、瞄点、触发方式、预测、Tracker 和输出开关。
- 控制测试：配置并测试 kmNet。
- 延迟分析：查看当前真实测量的推理 FPS、结果帧龄和统计窗口。

顶部状态栏会显示：

- 后端是否在线。
- 实时状态是否连接。
- 最近运行态更新时间。
- 异常信息入口。

看到“异常信息”出现数字时，优先点开查看具体原因。

## 6. 推荐日常流程

一次完整使用建议按下面顺序走：

1. 启动 `./NovaSight`，进入 Studio。
2. 完成授权。
3. 进入“采集”，确认视频设备和采集格式。
4. 将模型文件放入 `data/models/`。
5. 进入“模型推理”，打开“管理与切换模型”，选择 `.engine` 模型并切换。
6. 返回“采集”，确认 ROI 尺寸。
7. 进入“参数设置”，确认类别、瞄点、触发方式和输出开关。
8. 进入“控制测试”，确认 kmNet 可连接。
9. 点击顶部的“启动主链”。
10. 进入“控制”和“延迟分析”观察运行状态。

如果只是调参数，不建议一开始就打开物理输出。先确认采集、模型和目标选择稳定，再打开输出。

## 7. 采集设置

进入“采集”页面后，先确认“视频设备”。

常见默认值是：

```text
/dev/video0
```

如果换了采集卡或摄像头：

1. 修改“视频设备”。
2. 点击“检测设备能力”。
3. 在“采集格式”里选择合适的格式、分辨率和帧率。

建议优先选择：

- 实际稳定可用的帧率，而不是理论最高帧率。
- 与模型输入匹配更好的 ROI 尺寸。
- 延迟稳定的采集格式。

ROI 用来决定送入模型的画面区域。页面里可以调整 ROI 尺寸，常用值是 `320`、`480`、`640`。ROI 越大，看到的范围越大；ROI 越小，模型处理压力通常更低，但可能漏掉边缘目标。

修改采集设备、格式或 ROI 后，如果页面提示等待重启或运行值不同，请停止主链后重新启动。

## 8. 模型管理与切换

模型文件放在：

```text
data/models/
```

这里的 `data/models/` 指当前正在运行的 `NovaSight/` 交付包目录下面的路径。例如启动 `out/package/NovaSight/NovaSight` 时，模型目录就是 `out/package/NovaSight/data/models/`，不是仓库根目录的 `data/models/`。

推荐使用 `.engine` 模型文件。`.onnx` 文件可能会被目录识别，但当前正式运行主链以 `.engine` 为主要使用对象。

启动主链前必须先完成一次模型切换。Studio 顶部或“模型推理”页面如果显示“未发布模型”，表示还没有 active deployment，主链不会启动。

切换模型：

1. 进入“模型推理”。
2. 点击“管理与切换模型”。
3. 点击“刷新模型”。
4. 在模型目录中选择目标 `.engine` 文件。
5. 保持“解析兼容模式”为“自动识别（推荐）”，除非你明确知道模型类型。
6. 点击“验证并切换到所选模型”。
7. 等待验证流程完成。

模型标签：

- 可以把模型标记为“推荐”“待整理”“不推荐”。
- 可以添加“高精度模型”“低延迟”“稳定”等标签。
- 标签只用于整理目录，不会影响正在运行的推理主链。

如果切换失败，先查看弹窗中的失败阶段，再打开顶部“异常信息”和 `logs/novasightd.log`。

## 9. 推理预览与阈值

“模型推理”页面会显示：

- 推理 FPS。
- 识别结果 FPS。
- 结果新鲜度。
- 最近检测数量。
- 当前模型和输入尺寸。

常用可调项：

- 配置置信度：提高会减少低置信度目标，降低会看到更多候选。
- 配置 NMS：影响重叠框合并，通常不需要频繁修改。
- 推理画面预览：只用于观察 ROI 画面，关闭预览不会停止推理、跟踪或控制。

如果提示 ROI 被压缩到模型输入，说明输入区域比模型实际输入大很多，远距离小目标可能变差。优先考虑减小 ROI 或切换更大输入尺寸的模型。

## 10. 启动和停止主链

顶部按钮会根据状态显示“启动主链”或“停止主链”。

启动主链时，Studio 会显示启动进度：

1. 检查运行环境。
2. 应用采集配置。
3. 启动视觉处理链路。
4. 激活鼠标算法。

如果还没有发布当前模型，Studio 会先打开“模型管理与切换”，请选择一个 ready 的 `.engine` 并完成“验证并切换到所选模型”后再启动。

启动完成后：

- “采集”页应显示输入 FPS。
- “模型推理”页应显示推理或结果 FPS。
- “控制”页应显示目标选择和控制状态。

停止主链会停止采集、推理和控制计算。断开 kmNet 只停止物理输出通道，不等同于停止视觉主链。

## 11. 参数设置

“参数设置”页用于日常调校。

常用设置：

- 发送偏移控制量：总输出开关。关闭后仍会继续采集、推理和计算控制量，但不会发送物理位移。
- 触发方式：可选择“kmNet 硬件按键触发”或“检测到目标后自动控制”。
- 管理类别配置：设置哪些类别参与目标选择、类别名称、优先级，以及头部/身体/其他瞄点类型。
- 目标选择半径：控制候选目标离准星多远以内才参与选择。
- 调整权重：控制类别优先和距离优先的比例。
- 目标切换高级设置：控制目标切换的门槛和防抖。
- 管理 Tracker：控制跨帧身份关联，调得过松会误关联，调得过严会频繁断轨。

推荐调参顺序：

1. 先确认模型检测稳定。
2. 再确认类别过滤和目标优先级正确。
3. 再调目标选择半径。
4. 最后再调控制算法高级参数。

不要用 FOV 或每圈 counts 修手感。只有整体比例明显不对时，才检查这些标定项。

## 12. 视觉准星基准

“参数设置”页里有“视觉准星基准”。

用途：

- 从画面中心学习真实 HUD 准星位置。
- 当识别稳定时，用真实准星替代几何中心作为控制基准。
- 未学习、未确认或观测过期时，会自动回到 ROI 几何中心。

使用步骤：

1. 打开“启用低频准星观测”。
2. 停止并重新启动主链。
3. 等待学习帧数量达到要求。
4. 点击“学习当前准星 · F8”。
5. 确认“控制可用”为“是”后，再打开“用于目标选择与鼠标控制”。

如果准星模板不对，点击“清除模板”后重新学习。

## 13. kmNet 设置与控制测试

进入“控制测试”页面配置 kmNet。

常用字段：

- `kmnetip`：kmNet 设备地址。
- `kmnetport`：控制端口。
- `kmnetuuid`：设备 UUID。
- `monitor_port`：按键状态监控端口。
- 主链启动时连接设备：开启后，主链运行时自动连接 kmNet。

首次配置建议：

1. 点击“应用推荐参数”作为起点。
2. 按实际设备修改 IP、端口和 UUID。
3. 停止并重新运行 NovaSight，使新设备配置进入当前进程。
4. 启动主链。
5. 点击“连接 kmNet”。
6. 在“控制测试”页观察连接、按键和执行器状态。

单步诊断：

- 主链停止时，可以用方向按钮或“发送单步 raw”测试位移。
- 主链运行时，单步诊断会禁用，避免和实时控制通道竞争。

安全建议：

- 首次测试时先关闭“发送偏移控制量”。
- 确认目标选择稳定后，再打开输出。
- 需要立即阻断物理输出时，优先点击“断开 kmNet”或“停止主链”。

## 14. 延迟分析

“延迟分析”页只展示当前系统真实测到的数据，不显示猜测值。

重点看：

- 推理阶段耗时：从画面进入推理到输出识别结果的耗时。
- 结果帧龄：最新结果距离当前时间有多旧。
- 结果 FPS：检测结果输出速度。
- 统计窗口：当前指标的统计时间范围。

判断方式：

- 结果帧龄低于控制新鲜度阈值：通常可用于控制。
- 结果帧龄持续超过阈值：先降低预览帧率、减小 ROI，或换更轻的模型。
- 推理 FPS 正常但控制无目标：检查类别过滤、置信度、目标选择半径和模型输出。

## 15. 退出与更新

正常退出：

1. 在 Studio 顶部点击“停止主链”。
2. 等待状态变为停止。
3. 关闭浏览器页面。
4. 回到启动 `./NovaSight` 的终端，按 `Ctrl+C`。

如果原启动终端已经关闭，在交付包目录运行 `bin/novasightctl shutdown`。

更新新版本：

1. 停止旧版本。
2. 备份旧目录里的 `data/`，特别是 `data/models/`、`data/novasight.yaml` 和授权文件。
3. 解压或复制新的 `NovaSight/` 包。
4. 按需要把模型和配置放回新包的 `data/`。
5. 运行 `./NovaSight`。

不要在 NovaSight 正在运行时直接覆盖整个目录。

## 16. 常见问题

### 浏览器没有自动打开

查看终端输出，复制 `NovaSight Studio Web UI (LAN): ...` 到同一局域网电脑的浏览器。`0.0.0.0` 是监听地址，`127.0.0.1` 只代表 Jetson 本机；如果通过 SSH 在无桌面环境启动，自动打开浏览器失败是正常的。

### 页面显示后端不可达

确认 `./NovaSight` 还在运行；如果已经退出，查看 `logs/novasightd.log`。

### 无法进入工作台

先检查授权页状态。正式包需要有效授权；测试包可以尝试临时权限。

### 采集没有画面或没有 FPS

在“采集”页检查视频设备路径，点击“检测设备能力”，重新选择采集格式。仍不可用时，确认采集卡或摄像头在系统里可见。

### 模型目录为空

确认模型文件已经放入当前运行包的 `data/models/`，然后在“模型管理与切换”中点击“刷新模型”。如果你是从源码仓库运行 `out/package/NovaSight/NovaSight`，目录应是 `out/package/NovaSight/data/models/`。

### 提示 no active model deployment

这表示还没有任何 `.engine` 被发布为当前模型。进入“模型推理” -> “管理与切换模型”，选择 `.engine`，点击“验证并切换到所选模型”，成功后再启动主链。

### 模型切换失败

优先使用 `.engine` 模型。保持解析兼容模式为“自动识别（推荐）”。如果仍失败，查看切换弹窗、顶部“异常信息”和 `logs/novasightd.log`。

### 检测到了目标但不控制

依次检查：

1. “参数设置”里的“发送偏移控制量”是否打开。
2. 触发方式是否符合当前使用方式。
3. 类别配置是否允许当前检测类别。
4. 目标选择半径是否太小。
5. kmNet 是否已连接。

### kmNet 配置改了但没生效

部分 kmNet 配置需要重启当前 NovaSight 进程。停止主链，退出 `./NovaSight`，再重新启动。

### 需要立刻停止输出

优先操作：

1. 点击“断开 kmNet”。
2. 点击“停止主链”。
3. 关闭 `./NovaSight` 进程。

## 17. 可选命令

日常使用推荐通过 Studio 操作。下面命令只适合现场维护或排查时使用。

在 `NovaSight/` 目录内：

```bash
bin/novasightctl license status
bin/novasightctl status
bin/novasightctl emergency-stop
bin/novasightctl shutdown
```

说明：

- `license status`：查看授权状态。
- `status`：查看当前运行快照，需要已有有效授权。
- `emergency-stop`：立即关闭输出并停止运行主链。
- `shutdown`：关闭包内后台服务，适用于原启动终端已经不在的情况。

普通用户只需要记住一个启动命令：

```bash
./NovaSight
```

普通用户只需要用启动终端里的 `Ctrl+C` 退出。`novasightctl` 是现场维护入口，不是日常启动/退出路径。
